第一步：Application+stremingAssetsPath+定义的文件名
	获取存放bundle包的文件路径
	（可先判读是否有此包）
	获取文件夹的所有文件
	遍历文件进行后缀的判断
	在进行文件夹规则判断(也就是那些包是直接可以加载，那些需要手动)用过文件名进行规则匹配
	然后拿到这些ab包保存起来
第二步：资源的加载先获取自己的ab包名(也就是存放该ab包的父文件夹名的哈希值+规则)
	在进行比对是否有该包；
	取出该包进行资源名比对
	拿出包里的资源，并且存起来